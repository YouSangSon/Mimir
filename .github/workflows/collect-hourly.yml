name: collect-hourly
on:
  schedule:
    - cron: "37 * * * *"   # every hour at :37 (off-peak minute; cron is best-effort)
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_pipeline.yml
    with:
      cadence: hourly
    secrets: inherit
