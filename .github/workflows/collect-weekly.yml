name: collect-weekly
on:
  schedule:
    - cron: "41 22 * * 0"   # Sundays 22:41 UTC (off-peak minute; cron is best-effort)
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_pipeline.yml
    with:
      cadence: weekly
    secrets: inherit
