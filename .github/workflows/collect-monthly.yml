name: collect-monthly
on:
  schedule:
    - cron: "43 22 1 * *"   # 1st of month 22:43 UTC (off-peak minute; cron is best-effort)
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_pipeline.yml
    with:
      cadence: monthly
    secrets: inherit
