name: collect-daily
on:
  schedule:
    - cron: "23 21 * * 1-5"   # 21:23 UTC weekdays (off-peak minute; cron is best-effort)
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_pipeline.yml
    with:
      cadence: daily
    secrets: inherit
