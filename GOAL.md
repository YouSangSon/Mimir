# Goal

Continue improving Mimir one verified loop at a time until the user says stop.
Each loop must advance the free, legal public-data investment-insight pipeline,
fail closed when current official terms are incompatible with the intended use,
leave one runnable regression check, pass the repository gates, record the
decision, and end in an exact local commit without pushing or performing live
financial actions. Existing explicitly labeled GRAY-source policy remains a
separate operator-controlled boundary.
