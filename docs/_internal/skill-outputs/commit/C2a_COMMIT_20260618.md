# C2a 작업 분석

**Date:** 2026-06-18 KST
**Branch:** `feat/sec-cik-missing-map-path`

## Provenance

이 문서는 C2a captured news window cache 작업만 설명합니다. 앞선 SEC ticker CIK 오류 표면과 CLI stderr 계약 변경은 R1m/R1n 문서에 이미 기록되어 있습니다.

C2a 구현은 먼저 별도 커밋으로 들어갔고, 문서·메타데이터·추가 회귀 테스트는 post-review follow-up으로 정리했습니다.

| 구분 | 커밋/상태 | 범위 |
| ---- | --------- | ---- |
| 구현 커밋 | `0d92e158ab518fcc82cc2943e292342c37d0e654` | storage revision, reader cache, initial reader tests |
| follow-up 커밋 | 이 문서가 포함될 커밋 | README count, docs artifacts, catalog/backlog updates, `replace_partition()` invalidation regression |

## 구현 커밋 변경 파일

| File | Changes |
| ---- | ------- |
| `mimir/storage/jsonl_store.py` | `JsonlStore.revision`을 추가해 실제 저장 변경이 생겼을 때 reader cache를 무효화할 수 있게 함 |
| `mimir/storage/reader.py` | `DataReader.read_captured_window()`가 `captured_at.date()`별 메모리 index를 재사용하도록 변경 |
| `tests/analysis/test_reader.py` | 반복 captured-window 조회가 NEWS 전체 scan을 1회로 줄이고, append 뒤 cache가 갱신되는지 검증 |

## Follow-Up 변경 파일

| File | Changes |
| ---- | ------- |
| `tests/analysis/test_reader.py` | `replace_partition()` 뒤 captured-window cache가 교체된 NEWS partition을 다시 읽는지 검증 |
| `tests/test_readme_docs.py` | 최신 완료 ID 목록에 `C2a-CAPTURED-NEWS-CACHE`를 추가해 카탈로그 추적 누락을 방지 |
| `docs/architecture/improvement-catalog.md` | C2a 완료 상태와 broad C2 persistent index 보류 경계를 기록 |
| `docs/architecture/extensibility/README.md` | NEWS captured-window cache와 남은 persistent index 부채를 설명 |
| `docs/IMPROVEMENTS.md` | captured-window 반복 scan 완화 완료와 persistent index 후속 후보를 기록 |
| `docs/decisions/tech-spec/storage/C2a_captured_news_window_cache_tech_spec_2026_06_18.md` | C2a 설계, 비목표, 검증 결과를 한국어 tech spec으로 기록 |
| `docs/_internal/skill-outputs/jira-ticket/C2a-captured-news-window-cache.md` | unfamiliar reader용 ticket artifact를 생성 |
| `docs/superpowers/plans/2026-06-18-captured-news-window-cache.md` | TDD 기반 구현 계획과 completed status를 보존 |
| `README.md`, `README.ko.md`, `README.zh.md` | pytest collect count를 541로 갱신 |

## 커밋 메시지

구현 커밋:

```text
perf(analysis): cache captured news windows per reader
```

follow-up 커밋:

```text
test(analysis): cover captured cache partition replacement

- Add direct replace_partition invalidation coverage for the captured news cache
- Document the C2a reader cache boundary, verification, and remaining persistent index debt
- Update README health metadata and catalog drift guard for 541 collected tests
```

## 분석

- **Implementation type:** `perf` - 분석 결과 의미는 유지하면서 반복 NEWS scan 비용을 줄입니다.
- **Follow-up type:** `test` + `docs` - review에서 지적된 직접 회귀 테스트와 provenance 문서를 보강합니다.
- **Scope:** `analysis` - 뉴스 시그널이 사용하는 reader 경로의 실행 비용과 검증 표면을 다룹니다.
- **Files:** 구현 커밋 3개 + follow-up 테스트 2개 + follow-up 문서/메타데이터 10개
- **Verification:** collect-only, targeted pytest, storage pytest, README docs guard, full pytest, coverage, ruff, mypy, `git diff --check`
