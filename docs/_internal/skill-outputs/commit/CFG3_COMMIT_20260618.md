# CFG3 Commit Message

## 변경 요약

로컬 운영자 설정 가드레일을 추가합니다. `watchlist.yaml`은 schema 검증으로 symbol 무음 손상을 막고, `sources.yaml`의 `llm_sentiment_max_headlines`는 유료 LLM 호출량을 `1`~`50`으로 제한합니다. 잘못된 watchlist는 `[mimir] invalid watchlist.yaml:` 메시지와 exit code 1로 실패합니다.

## Commit 이력 (provenance)

CFG3은 세 커밋으로 나뉩니다.

| 커밋 | 범위 | 내용 |
| ---- | ---- | ---- |
| `785fcf9` | test | watchlist schema 검증 재현 테스트(RED) 추가 |
| `9ae23f6` | fix(config) | `watchlist.yaml` schema 검증 + CLI friendly error 구현 |
| `8ae1246` | fix(config) | `llm_sentiment_max_headlines` cap 경계(`ge=1, le=50`) 추가 |
| (이 커밋) | docs(config) | as-built 동작 문서화 + README navigation guard + 추적 산출물 |

## 변경 파일 (Task 1 — watchlist schema)

| 파일 | 변경 |
| ---- | ---- |
| `mimir/config.py` | `WatchlistConfigError`, `_WatchlistConfig`, `report_invalid_watchlist()` 추가 |
| `mimir/collect.py`, `mimir/analyze.py`, `mimir/run.py`, `mimir/backfill.py`, `mimir/dashboard.py`, `mimir/history.py`, `mimir/doctor/doctor_cli.py` | watchlist 로드 경로에 friendly error 처리 |
| `tests/test_config.py`, `tests/test_collect.py` | schema 거부·정규화·CLI 오류 회귀 테스트 |

## 변경 파일 (Task 2 — LLM headline cap)

| 파일 | 변경 |
| ---- | ---- |
| `mimir/sources/config.py` | `SourcesConfig`·`_TopLevelSourcesConfig`의 `llm_sentiment_max_headlines`에 `Field(default=50, ge=1, le=50)` |
| `tests/sources/test_config.py` | `0`/`-1`/`51` 거부, `1`/`50` 허용, 직접 생성 경계 회귀 테스트 |

## 변경 파일 (Task 3 — docs)

| 파일 | 변경 |
| ---- | ---- |
| `docs/reference/config/watchlist.md` | `watchlist.yaml` 레퍼런스 신규 |
| `docs/decisions/tech-spec/config/CFG3_config_guardrails_tech_spec_2026_06_18.md` | CFG3 tech spec 신규 |
| `docs/decisions/tech-spec/README.md` | tech-spec 색인 신규 |
| `docs/reference/config/sources.md` | `llm_sentiment_max_headlines` 범위(1~50)와 잘못된 예시 |
| `docs/architecture/roadmap.md` | 최신 결정 spec이 `docs/decisions/tech-spec/`에도 위치 가능 |
| `docs/architecture/extensibility/README.md` | SEC RSS YAML 예시에 `ticker_cik_map_path` 추가 |
| `docs/architecture/improvement-catalog.md`, `docs/IMPROVEMENTS.md` | CFG3 완료 범위 추가 |
| README 3종 | Further Reading에 catalog·tech-spec index·watchlist 링크 추가, pytest 수집 개수 553으로 갱신 |
| `tests/test_readme_docs.py` | README navigation guard 테스트 추가 |

## Commit

```bash
git commit -m "docs(config): document config guardrails"
```

## Rationale

- **Type:** `docs` - 이 커밋은 Task 1/2의 as-built 동작 문서화와 README navigation guard 추가입니다.
- **Scope:** `config` - watchlist/sources 설정 가드레일 범위입니다.
- **Verification:** `pytest --collect-only`, `tests/test_readme_docs.py`, `ruff check .`, `mypy mimir`, `git diff --check` 통과
