# CFG2 Commit Message

## 변경 요약

`mimir doctor`가 데이터 점검이나 HTML 파일 쓰기 전에 `sources.yaml` schema를 검증하도록 했습니다. 잘못된 설정은 다른 CLI와 같은 `[mimir] invalid sources.yaml:` 메시지와 exit code 1로 실패합니다.

## 변경 파일

| 파일 | 변경 |
| ---- | ---- |
| `mimir/doctor/doctor_cli.py` | `load_validated_sources_config()`를 doctor 실행 전에 호출 |
| `tests/doctor/test_cli.py` | malformed config에서 friendly error와 no HTML write 회귀 테스트 추가 |
| README 3종 | pytest 수집 개수 536으로 갱신 |
| `docs/reference/config/sources.md` | doctor도 schema 검증을 먼저 수행한다고 문서화 |
| `docs/architecture/improvement-catalog.md`, `docs/IMPROVEMENTS.md` | CFG2 완료 범위 추가 |
| CFG2 ticket/spec/commit artifacts | 추적 가능한 산출물 추가 |

## Commit

```bash
git commit -m "fix(doctor): validate sources config before checks" \
  -m "Validate sources.yaml at the doctor CLI boundary before reading data or writing HTML output." \
  -m "Add regression coverage for malformed sources config and document the CFG2 operational contract."
```

## Commit Body

```text
fix(doctor): validate sources config before checks

Validate sources.yaml at the doctor CLI boundary before reading data or
writing HTML output.

Add regression coverage for malformed sources config and document the CFG2
operational contract.
```

## Rationale

- **Type:** `fix` - doctor가 설정 오류를 놓치던 운영 오류 표면을 수정합니다.
- **Scope:** `doctor` - 핵심 변경이 doctor CLI boundary에 있습니다.
- **Verification:** doctor/readme targeted tests 12 passed, `ruff`, `mypy`, full pytest 536 passed, coverage TOTAL 98%, `git diff --check` 통과
